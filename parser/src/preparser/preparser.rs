use crate::parsingdata::{
    ParsingData,
    Block,
    Expression,
};

fn preparser() -> Vec<ParsingData>{
    let mut retval : Vec<ParsingData> = Vec::new();
    // currently this function is useless
    // @todo : Conplete this fucking thang 
    return retval;
}
